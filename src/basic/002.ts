const PI = 3.1415; // -> Usamos o const usado para declrar valores que serão constantes em todo programa.

function soma() {
  let n1 = 20;
  let n2 = 30;
  let soma = n1 + n2;
  // -> Usamos o let para declrar variaveis que serão utilizadas dentro de um escopo do nosso programa
}

var idade = 21
// -> Usamos o var para declarar variaveis que serão acessadas em qualquer parte do nosso código.

export default {};
